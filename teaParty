public int teaParty(int tea, int candy) {
  //-------------------self try------------------
  // if((tea>=5 && candy>=5)){
  //   if(tea*2<=candy || candy*2<=tea)    return 2;
  //   return 1;
  // }
  // return 0;
  
  if(tea<5 || candy<5){
    return 0;
  }
  if(tea>=candy*2 || candy>=tea*2){
    return 2;
  }
  return 1;
}
