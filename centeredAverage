public int centeredAverage(int[] nums) {
  int sum=nums[0];
  int max=nums[0];
  int min=nums[0];
  for(int i=1;i<nums.length;i++){
    if(max<nums[i]){
      max=nums[i];
    }
    if(min>nums[i]){
     min=nums[i]; 
    }
    sum+=nums[i];
  }
  sum-=(max+min);
  return sum/(nums.length-2);
}
